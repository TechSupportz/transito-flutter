import 'package:flutter/material.dart';

class FavouritesTimingCard extends StatelessWidget {
  const FavouritesTimingCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
